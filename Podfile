platform :ios, '10.0'
swift_version = '4.2'
use_frameworks!

# ignore all warnings from all pods
inhibit_all_warnings!

target 'SelfResizingCollectionViewTutorial' do
    pod 'Kingfisher', '~> 5.0'
end
